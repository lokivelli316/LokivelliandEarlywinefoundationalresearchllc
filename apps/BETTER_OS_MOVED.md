# Better OS Dashboard moved

The Better OS Dashboard source of truth now lives in the dedicated private repository `lokivelli316/better-os-dashboard`.

The copy formerly stored under `apps/better-os-dashboard/` was removed to prevent the LLC website repository and the standalone application from diverging.
